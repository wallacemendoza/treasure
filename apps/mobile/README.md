# apps/mobile

Not configured yet. This will hold the React Native + Expo (TypeScript)
mobile app, using Expo Router and sharing types/logic with `packages/shared`.

Configured after apps/web, in the "Configure React Native + Expo" step —
not yet.
