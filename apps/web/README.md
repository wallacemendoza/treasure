# apps/web

Not configured yet. This will hold the React + Vite (TypeScript) web app,
built by converting the Phase 1 JS Vite app to TypeScript and wiring it to
`packages/shared`.

Configured in the next step ("Configure React + Vite") — not yet.
