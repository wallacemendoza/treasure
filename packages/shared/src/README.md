# packages/shared

Not configured yet. This will hold TypeScript types, validation schemas,
Supabase query helpers, permission logic, constants (member ranks, access
roles, design tokens), and date utilities — anything with no browser-only
or React Native-only dependency.

Configured in the next step ("Configure the shared package") — not yet.
